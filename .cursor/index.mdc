---
description: Root .cursor index — FCHIP SoT and conflict order
globs:
  - ".cursor/**"
  - "backend/**"
  - "frontend/**"
alwaysApply: false
---

# FCHIP Rules Index

FCHIP is the Community Health Intelligence Platform monorepo (`backend/` + `frontend/`).

## Source of truth

[`.cursor/app-write-up.mdc`](app-write-up.mdc) is the **only** product SoT (always apply). It owns:

- FCHIP product positioning (intelligence platform on the cascade Data & Feedback loop)
- Community health cascade, indicators, partner pathways, community journey (section 2)
- Canonical terms: FCHIP, cascade, CHIS (optional data domain), CHW/VHT, EMR/HMS, GIS + climate APIs
- Vision, MVP, funding copy, and do-not-invent rules

Slogan: **Your health, our mission.** Mission: Connecting Care, Empowering Communities, Creating Lasting Impact.

## Conflict order

1. Security, privacy, safety, and compliance.
2. `.cursor/app-write-up.mdc` for product identity, cascade, and scope.
3. `.cursor/mandatories.mdc` for shipping checks, including frontend/backend pairing.
4. `frontend/.cursor/index.mdc` or `backend/.cursor/index.mdc` for stack guidance.
5. `dev-plan/` steps for chronology only.

## Delivery model

Product work is frontend-first and paired: build the Flutter screen from `app-ui/`, then ship its backend in the same vertical slice. Order lives in `frontend/dev-plan/24-product-vertical-slices.md`, the per-screen loop in `25-slice-execution-playbook.md`, and status in `frontend/dev-plan/slices/tracker.md`. Verify with `python tool/check_slice_coverage.py`.

## Keep / do not invent

- Frame FCHIP as the Community Health Intelligence Platform — not a generic hospital or EMR brand.
- EMR/HMS means **external** facility systems FCHIP interoperates with via secure data APIs.
- Do not invent alternate cascade order or programme names — follow SoT section 2.

## Root `.cursor` helpers

| File | Role |
| --- | --- |
| `app-write-up.mdc` | Product SoT |
| `mandatories.mdc` | Shipping checks on every change |
| `rule-file-standards.mdc` | How to write `.mdc` rules |
| `locale-development.mdc` | English-first locale edits |

## Stack indexes

- `frontend/.cursor/index.mdc` — Flutter architecture, UI, data flow.
- `backend/.cursor/index.mdc` — Express, Prisma, API, security.

## Application packs

Shared grant/doc rules: `.cursor/rules/` (`documents.mdc`, `folder_structure.mdc`, `instructions.mdc`). Narrative still comes from `app-write-up.mdc`.
