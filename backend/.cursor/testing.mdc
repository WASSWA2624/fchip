---
description: Backend tests for schemas, authorization, subscription intersection, and workflow regression.
globs:
  - "backend/**"
alwaysApply: false
---
# Testing Rules

Define the minimum backend verification bar.

## Required coverage
- schema validation tests
- repository tests
- service tests
- controller and route tests
- auth, entitlement, ABAC, and break-glass regression tests
- `union(role, module, user grants) ∩ subscription ∩ assigned modules ∩ ABAC scope` regression tests
- API and websocket contract tests proving responses/events expose `human_friendly_id` only
- admin customize/reset of access defaults producing auditable, atomic changes
- seed and maintenance script tests where scripts change data or contracts

## Requirements
- Every module must mirror its runtime structure in tests.
- New modules are incomplete until tests and docs land together.
- High-risk domains (identity, consent, PHI sharing, referrals, connector access, alerts, offline conflicts, and anonymised exports) need workflow-level regression coverage.
- Every product slice needs a contract test using the same fixture shape as its frontend repository and an integration test for its declared UI states.
- Mocks must preserve real contract shape for Prisma, storage, websocket, and auth dependencies.
