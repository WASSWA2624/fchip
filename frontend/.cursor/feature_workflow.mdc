---
description: Define the repeatable process for adding a new frontend feature.
globs:
  - "frontend/lib/features/**"
alwaysApply: false
---

# Feature Workflow

Repeatable process for adding a feature to the FCHIP frontend.

## Requirements

- Follow [`product_delivery.mdc`](./product_delivery.mdc): begin with the next unfinished chronology screen in `frontend/dev-plan/slices/chronology.yaml`, then its `screen.json`, six visual references, and connected `app-flows/` journey.
- Start by identifying route, declared states, roles, access, actions, data needs, validation, localization, responsive behavior, offline/realtime needs, and tests.
- Implement the responsive Flutter UI with typed fixtures first; then define and deliver the smallest matching backend in the same atomic unit, using the derivation table in `frontend/dev-plan/25-slice-execution-playbook.md`.
- Do not open the next chronology screen while this one has an undocumented backend gap.
- Create the feature folder with the standard `data/domain/presentation` structure.
- Define domain models and repository contracts before wiring UI to real data when non-trivial.
- Add localization keys before adding user-facing text.
- Reuse shared components before creating feature-specific widgets.
- Queue/workspace features must confirm layout against [`ui-workspace.mdc`](./ui-workspace.mdc) and a reference module first.
- Confirm subscription-gated access and permission gates before exposing routes or actions ([`permissions.mdc`](./permissions.mdc)).
- Verify mobile, tablet, and desktop layouts; must not hard-code colors, spacing, or typography ([`design-system.mdc`](./design-system.mdc)).
- Reference entities by `human_friendly_id`; must not expose raw table IDs.
- Enforce RBAC+ABAC visibility for menus, components, and actions.
- Shared-data mutations must update Riverpod immediately and reconcile via [`instant_ui_sync.mdc`](./instant_ui_sync.mdc).
- Add tests for controller logic, repository mapping, and important widgets.
- Add backend contract/workflow tests and one cross-stack integration test before closing the slice.
- Update docs when structure or conventions change.

## Acceptance

- The feature works at supported breakpoints with no boundary violations.
- Queue screens match [`ui-workspace.mdc`](./ui-workspace.mdc) when applicable.
- Tests and localization are included.
- The real backend contract is wired, or the tracker explicitly proves the screen is static and needs no backend.

## Related

- [`ui-workspace.mdc`](./ui-workspace.mdc)
- [`product_delivery.mdc`](./product_delivery.mdc)
- [`architecture.mdc`](./architecture.mdc)
- [`components.mdc`](./components.mdc)
- [`testing.mdc`](./testing.mdc)
